# 코바몰 고도몰5 스킨 작업 지침

## 목표

고도몰5의 기존 쇼핑 기능과 관리자 연동을 보존하면서 PC·모바일 스킨을 점진적으로 리뉴얼한다. 현재 구조와 공식 근거는 작업 전에 `CONTEXT.md`에서 확인한다.

## 작업 대상

- PC: `작업장/designbook_plen_backup`
- 모바일: `작업장/designbookM_plen_backup`
- 원본: `skin_backup/`의 ZIP 파일. 수정·삭제·덮어쓰기 금지.
- 우선 확인: `main/index.html`, `outline/`, `_dbook/`, PC `css/custom.css`

## 변경 원칙

1. 요청한 화면과 직접 관련된 파일만 수정한다. 인접 페이지 정리나 공통 라이브러리 교체는 하지 않는다.
2. PC와 모바일은 별도 스킨이다. 한쪽을 수정하면 다른 쪽의 동일 사용자 흐름도 확인하고, 변경 필요 여부를 명시한다.
3. `{=...}`, `{...}`, `{ # ... }`, `<!--{ ? ... }-->`, `<!--{ @ ... }-->`, `<!--{ : }-->`, `<!--{ / }-->` 등 고도몰 치환식·조건·반복·include 문법을 보존한다.
4. 새 치환코드를 추측해 만들지 않는다. 기존 동일 패턴이나 공식 문서로 확인할 수 없으면 사용자에게 필요한 관리자 정보를 요청한다.
5. HTML 전체 포매팅, CSS 전역 치환, 번들 라이브러리 업그레이드는 요청 없이 수행하지 않는다.
6. 배너 코드, 상품 진열 `sno`, 게시판 ID, 경로, 폼 필드, CSRF·결제·회원 관련 코드는 디자인 요소로 보고 임의 변경하지 않는다.
7. 비밀번호, FTP 정보, API 키, 개인정보를 파일에 기록하지 않는다.
8. **PC/모바일 동시 수정 원칙**: UI 또는 문구 변경 요청은 항상 PC와 모바일 대응 영역을 함께 확인하며, 한쪽만 변경해야 하는 명확한 이유가 없다면 양쪽을 같이 수정·검토한다. 단, PC와 모바일 사이의 `sno`, banner ID, include 경로 등은 서로 복사하지 않고 각 스킨의 기존 연결값을 보존한다.
9. **관리자 데이터 우선 원칙**: `gMall.*` 등 관리자 연동 정보는 하드코딩으로 대체하지 않는다. 상품 구성, 배너 구성, 회사 기본정보처럼 관리자에서 수정 가능한 값은 관리자 연동을 우선하며, 코드에 잘못 하드코딩된 이전 브랜드 정보만 실제 운영값으로 교체한다.

## 작업 흐름

1. 대상 화면과 PC/모바일 범위를 정한다.
2. 관련 템플릿의 include, 치환코드, CSS·JS 연결을 추적한다.
3. 작은 변경 단위로 수정한다.
4. 문법 검색과 변경 파일 diff를 확인한다.
5. 가능한 경우 실제 고도몰 작업 스킨에서 데스크톱·모바일 화면과 핵심 상호작용을 검수한다.
6. 관리자 적용 전 백업, 작업 스킨 지정, 실제 사용 스킨과의 구분을 확인한다.

## 검증 기준

- 치환문법의 여닫기와 include 경로가 유지된다.
- 브라우저 콘솔 오류, 깨진 이미지·링크, 레이아웃 넘침이 없다.
- 375px, 768px, 1280px 기준으로 대상 화면을 확인한다. PC·모바일 전용 스킨의 실제 적용 조건을 우선한다.
- 로그인, 장바구니, 주문, 결제, 회원정보처럼 운영 데이터가 필요한 흐름은 로컬 추측으로 통과 처리하지 않는다.
- 로컬 빌드 시스템이 없으므로 "빌드 성공" 대신 변경 파일 검사와 작업 스킨 실화면 검수를 증거로 남긴다.

## 컨텍스트 관리

- 확정된 운영 설정, 활성 스킨 ID, 배포 방식, 디자인 결정은 `CONTEXT.md`에 짧게 추가한다.
- 디자인 리뉴얼이 실제 시작될 때만 `DESIGN.md`를 만든다. 색상·서체·간격·컴포넌트 규칙이 생기기 전에는 만들지 않는다.
- 하위 `AGENTS.md`, 패키지 매니저, 프런트엔드 프레임워크, 자동화 스크립트는 반복 필요가 확인되기 전까지 추가하지 않는다.

## 사용할 스킬

- UI 설계·수정: `omo:frontend`
- 구현 후 실제 화면 검수: `omo:visual-qa`
- 고도몰 공식 자료 조사: `(matt)research`
- 브라우저에서 작업 스킨 확인: `browser:control-in-app-browser`

작업에 맞는 스킬을 호출하고, 각 스킬 안에서는 요청 범위를 충족하는 최소 경로만 선택한다.
